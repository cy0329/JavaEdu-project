package com.mis.web;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.ModelAttribute;
import org.springframework.web.bind.annotation.RequestMapping;

@Controller
public class SampleController {
	
	private static final Logger logger = LoggerFactoryry.getLogger(SampleController.class);
	
	/**
	 * 1) void 타입 -> doA.jsp로 응답
	 */
	@RequestMapping("doA")
	public void doA() {
		
		// 요청 -> 응답 (화면으로, .jsp)  ->  나의 requestMapping 주소와 동일한 jsp를 실행하겠다. -> doA.jsp
		logger.info("doA called..............");
		
	}
	
	
	/**
	 * 2) 리턴 타입 String -> 기술한 문자열.jsp로 응답 -> hoem.jsp
	 * @return 
	 */
	@RequestMapping("doB")
	public String doB() {
		
		
		return "home";
		
	}
	
	/**
	 * 3) 리턴 타입 String -> 기술한 문자열.jsp로 응답 -> result.jsp + 매개변수 msg 전달
	 * @param msg
	 * @return
	 */
	@RequestMapping("doC")
	public String doC(@ModelAttribute("msg") String msg) {
		
		logger.info("doC called............");
		return "result";
		
	}

}
